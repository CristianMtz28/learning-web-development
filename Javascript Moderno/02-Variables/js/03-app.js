//Declarando variables con const

//Diferencias entre let y const es que const no puede ser reasignado, de ahí su abreviación de constante

const producto = 'iPhone';

//producto = 'Tablet';

//Las variables con const deben forzosamente a huevo iniciar con un valor, a diferencia de let o var que podemos poner var producto; o let producto; con const no se puede

const nombre = 'Producto';

//Si no conocemos el valor puedes usar let, pero normalmente se usa más constantemente const en los proyectos