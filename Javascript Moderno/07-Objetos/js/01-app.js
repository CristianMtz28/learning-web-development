/*Los objetos son la pieza clave de Javascript ya que en vez de crear muchísimas variables
se crean objetos con más atributos que se asignan a estos*/
//En vez de crear

const nombre = `Monitor de 20 pulgadas`;
const precio = 300;
const disponible = true;

//En vez de crear uno por uno podemos crear un objeto, ya que un objeto agrupa todo en una misma variable:

const producto = {
    nombre: `Monitor de 20 pulgadas`,
    precio: 300,
    disponible: true
}

console.log(producto);